import { Component } from '@angular/core';

/**
 * PLACEHOLDER (T-M1-7): rota pública `/login` para o wiring do núcleo de auth.
 * A tela real (form reativo + design distintivo) é entregue na T-M1-8.
 */
@Component({
  selector: 'app-login',
  template: `<p class="placeholder">Login — em construção (T-M1-8).</p>`,
})
export class Login {}
