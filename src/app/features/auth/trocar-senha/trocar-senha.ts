import { Component } from '@angular/core';

/**
 * PLACEHOLDER (T-M1-7): rota autenticada `/trocar-senha` para o wiring do núcleo de auth.
 * O form real (senha atual + nova + 1º login forçado) é entregue na T-M1-10.
 */
@Component({
  selector: 'app-trocar-senha',
  template: `<p class="placeholder">Troca de senha — em construção (T-M1-10).</p>`,
})
export class TrocarSenha {}
