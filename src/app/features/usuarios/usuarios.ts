import { Component } from '@angular/core';

/**
 * PLACEHOLDER (T-M1-7): rota `/usuarios` (adminGuard) para o wiring do núcleo de auth.
 * A gestão real (lista/criar/ativar-desativar/reset) é entregue na T-M1-9.
 */
@Component({
  selector: 'app-usuarios',
  template: `<p class="placeholder">Gestão de usuários — em construção (T-M1-9).</p>`,
})
export class Usuarios {}
